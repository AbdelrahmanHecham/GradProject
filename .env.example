# Port your Express app will listen on
PORT=3000

# Session secret (replace with a long, random string)
SESSION_SECRET=changeme_to_a_strong_secret

# Path to your SQLite DB (relative to project root)
DATABASE_PATH=data/users.db